---
sidebar_label: Installing Saarthi
description: Step-by-step guide to install Saarthi in VS Code. Get started with AI-powered coding in minutes.
keywords:
  - install Saarthi
  - VS Code extension
  - AI coding assistant installation
  - Cursor
  - VSCodium
---
import SaarthiIcon from '@site/src/components/SaarthiIcon';

# Installing Saarthi
<div style={{ position: 'relative', paddingBottom: '56.25%', height: 0, overflow: 'hidden' }}>
  <iframe
    src="https://www.youtube.com/embed/QFXn8Xv1gLY?si=VdodMDS1rUqLZOHk"
    style={{
      position: 'absolute',
      top: 0,
      left: 0,
      width: '90%',
      height: '90%',
    }}
    frameBorder="1"
    allow="autoplay; encrypted-media" allowFullScreen></iframe>
</div>



<SaarthiIcon /> [Saarthi](https://marketplace.visualstudio.com/items?itemName=godspeed-systems.saarthi) is a VS Code extension that brings AI-powered coding assistance directly to your editor. Install from
**VS Code Marketplace** - fastest method for standard VS Code users

<!--
Saarthi uses Python to enable its advanced AI and framework-level features (like RAG-based querying, local agents, and the MCP server).
### What You Need to Do:
1. **Check if Python is installed**
   Run this in your terminal:
   ```bash
   python --version
   ```
2. **If not installed**, download and install Python from the official website:
   👉 [Download Python](https://www.python.org/downloads/)
3. **Important**:
   During installation, **make sure to check the box**:
   > ✅ **Add Python to PATH**
---
:::tip
Without Python, the installation of Saarthi and its components like the **rag-node MCP server** will not work.
:::
![Installing Saarthi](/img/installing/installing.png)
-->

## Steps To Install

1. Open VS Code
2. Click the Extensions icon on the sidebar or press `Ctrl+Shift+X` (Windows/Linux) or `Cmd+Shift+X` (macOS)
3. In the search bar, type “Saarthi”.  Select <SaarthiIcon/> Saarthi by godspeed and click **Install**
4. Reload VS Code if prompted
5. Click on Saarthi icon, you'll then see Saarthi welcome screen.
5. Here, Select LLM Provider and enter API Key. For help on connecting your API provider [ssCheck this](/docs/saarthi/getting-started/connecting-api-provider)
- To use Free Google API Key. Get it from [Google AI Studio](https://aistudio.google.com/app/apikey)


## Enable Godspeed Context Awareness

To get Godspeed-specific suggestions, documentation-aware answers, and context-driven support, 
you’ll need to install the Godspeed-powered RAG-MCP Server.

**This is highly recommended for Godspeed framework users.**

Run the following command based on your OS:

#### Windows (PowerShell):

```powershell
powershell -ExecutionPolicy Bypass -Command "iwr -useb 'https://github.com/godspeedsystems/RAG-MCP-Server/releases/download/0.0.1/setup-saarthi.ps1' | iex"
```

#### macOS/Linux (Bash):

```bash
bash <(curl -sSL "https://github.com/godspeedsystems/RAG-MCP-Server/releases/download/0.0.1/setup-saarthi.sh")
```

This will install:

- The **MCP server**
- A local vector store (Chromadb)
- Saarthi’s context pipeline for deep Godspeed integration

That’s it ! You’re now ready to transform your development workflow the 10x way using **Saarthi + Godspeed**.



## Troubleshooting

*VS Code Output panel showing Saarthi logs for troubleshooting*

**Extension Not Visible**
* Restart VS Code
* Verify Saarthi is listed and enabled in Extensions
* Try disabling and re-enabling
* Check Output panel for errors (View → Output, select "Saarthi")

**Installation Problems**
* Ensure stable internet connection
* Reload VS Code in case installation takes more than 10 times.


